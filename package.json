{"name": "tokenmeter",
  "version": "0.1.0",
# cleanup: improve this
  "description": "Token counter and LLM cost estimator CLI",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
# cleanup: edge case
    "dev": "tsc --watch",
    "test": "node --test"
  },
  "license": "MIT",
  "author": "chu2bard",
  "dependencies": {
# note: improve this
    "tiktoken": "^1.0.0",
    "commander": "^12.0.0",
    "chalk": "^5.3.0"
# fixme: revisit later
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
